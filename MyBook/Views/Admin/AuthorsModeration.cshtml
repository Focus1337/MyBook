@model IEnumerable<MyBook.Entity.Author>
@{
    ViewBag.Title = "Авторы - ADMIN";
    var counter = 0;
    Layout = "_Layout1";
}

<div class="container">
    <div class="row mt">
        <div class="col-lg-6 col-lg-offset-3 centered">
            <h3>Модерация авторов</h3>
            <hr>
        </div>
    </div>
    <div class="row">
        <div class="col-md-offset-1 col-md-10">
            <div class="panel">
                <div class="panel-heading">
                    <div class="row">
                        <div class="col-sm-12 col-xs-12 d-flex justify-content-between">
                            <a href="#" class="btn btn-sm btn-primary pull-left">
                                <i class="glyphicon glyphicon3 glyphicon-plus-sign"></i> Добавить нового автора
                            </a>
                            
                            <div>
                                <a asp-controller="Admin" asp-action="BooksModeration" class="btn btn-sm btn-primary pull-right">
                                    <i class="glyphicon glyphicon3 glyphicon-check"></i> Модерация книг
                                </a>
                                <a asp-controller="Admin" asp-action="Index" class="btn btn-sm btn-primary pull-right">
                                    <i class="glyphicon glyphicon3 glyphicon-check"></i> Модерация пользователей
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="panel-body table-responsive">
                    <table class="table">
                        <thead>
                        <tr>
                            <th>#</th>
                            <th>Имя Фамилия</th>
                            <th>Действия</th>
                        </tr>
                        </thead>
                        <tbody>
                        @foreach (var author in Model)
                        {
                            <tr>
                                <td>@(counter++)</td>
                                <td>@author.FullName</td>
                                <td>
                                    <form href="#" method="post">
                                        <a class="btn btn-sm btn-primary" href="#">Изменить</a>
                                        <button type="submit" class="btn btn-sm btn-danger">
                                            Удалить
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>